import { useEffect,useState } from 'react'; import api from '../services/api'; import { useAuth } from '../context/AuthContext';
export default function TasksPage(){const{user}=useAuth();const[tasks,setTasks]=useState([]);const[projects,setProjects]=useState([]);const[members,setMembers]=useState([]);const[msg,setMsg]=useState('');const[form,setForm]=useState({title:'',description:'',status:'Pending',dueDate:'',projectId:'',assignedTo:''});const load=()=>api.get('/tasks').then(r=>setTasks(r.data));useEffect(()=>{load();api.get('/projects').then(r=>setProjects(r.data));if(user?.role==='admin'){api.get('/tasks/members/list').then(r=>setMembers(r.data));}},[user]);const create=async(e)=>{e.preventDefault();try{await api.post('/tasks',form);setMsg('Task created');setForm({title:'',description:'',status:'Pending',dueDate:'',projectId:'',assignedTo:''});load();}catch(err){setMsg(err.response?.data?.message||'Error')}};const update=async(id,status)=>{await api.put(`/tasks/${id}`,{status});load();};return <div><h1 className='text-2xl font-bold mb-4'>Tasks</h1>{msg&&<p>{msg}</p>}{user?.role==='admin'&&<form onSubmit={create} className='bg-white p-4 rounded shadow mb-4 grid md:grid-cols-2 gap-2'><input className='border p-2 rounded' placeholder='Task title' value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input type='date' className='border p-2 rounded' value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})}/><textarea className='border p-2 rounded md:col-span-2' placeholder='Description' value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><select className='border p-2 rounded' value={form.projectId} onChange={e=>setForm({...form,projectId:Number(e.target.value)})}><option value=''>Select Project</option>{projects.map(p=><option key={p.id} value={p.id}>{p.title}</option>)}</select><select className='border p-2 rounded' value={form.assignedTo} onChange={e=>setForm({...form,assignedTo:Number(e.target.value)})}><option value=''>Select Member</option>{members.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select><button className='bg-blue-600 text-white px-3 py-1 rounded md:col-span-2'>Create Task</button></form>}<div className='space-y-2'>{tasks.map(t=><div key={t.id} className='bg-white p-4 rounded shadow'><h3 className='font-semibold'>{t.title}</h3><p>{t.description}</p><p>Project: {t.projectTitle} | Assignee: {t.assigneeName}</p><p>Due: {t.dueDate}</p>{(user?.role==='admin'||true)&&<select className='border p-1 rounded mt-2' value={t.status} onChange={e=>update(t.id,e.target.value)}><option>Pending</option><option>In Progress</option><option>Completed</option></select>}</div>)}</div></div>}
