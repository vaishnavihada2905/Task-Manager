import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import api from '../services/api'; import { useAuth } from '../context/AuthContext';
export default function LoginPage(){const [form,setForm]=useState({email:'',password:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);const nav=useNavigate();const{login}=useAuth();const submit=async(e)=>{e.preventDefault();setError('');setLoading(true);try{const {data}=await api.post('/auth/login',form);login(data);nav('/dashboard');}catch(err){setError(err.response?.data?.message||'Login failed');}finally{setLoading(false)}};return <div className='min-h-screen flex items-center justify-center'><form onSubmit={submit} className='bg-white p-6 rounded shadow w-full max-w-md space-y-4'><h1 className='text-2xl font-bold'>Login</h1>{error&&<p className='text-red-500'>{error}</p>}<input className='w-full border p-2 rounded' placeholder='Email' value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input type='password' className='w-full border p-2 rounded' placeholder='Password' value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className='w-full bg-blue-600 text-white p-2 rounded'>{loading?'Loading...':'Login'}</button><p>New user? <Link to='/signup' className='text-blue-600'>Signup</Link></p></form></div>}
